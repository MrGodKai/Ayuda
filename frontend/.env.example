# URL base del API usada por el frontend.
# En desarrollo se recomienda dejar /api para usar el proxy de Vite.
VITE_API_URL=/api

# Destino real del proxy de Vite para /api.
VITE_API_PROXY_TARGET=http://localhost:3001
